import { memo } from "react";
import { Main } from "../../../widgets/Main";

const MainPage = memo(() => {
    return (
        <div>
            <Main />
        </div>
    );
});

export default MainPage